import SwiftUI

struct BillsScreen: View {
    var body: some View {
        ZStack {
            Color.white.ignoresSafeArea()
            Text("Bills Screen")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
    }
}

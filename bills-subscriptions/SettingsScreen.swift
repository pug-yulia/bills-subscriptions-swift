import SwiftUI

struct SettingsScreen: View {
    var body: some View {
        ZStack {
            Color.white.ignoresSafeArea()
            Text("Settings Screen")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
    }
}

import SwiftUI

struct AddPaymentScreen: View {
    var body: some View {
        ZStack {
            Color.white.ignoresSafeArea()
            Text("Add Payment Screen")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
    }
}

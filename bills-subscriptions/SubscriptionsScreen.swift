import SwiftUI

struct SubscriptionsScreen: View {
    var body: some View {
        ZStack {
            Color.white.ignoresSafeArea()
            Text("Subscriptions Screen")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
    }
}
